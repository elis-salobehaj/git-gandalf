services:
  git-gandalf:
    build: .
    env_file: .env
    ports:
      - "${PORT:-8020}:${PORT:-8020}"
    volumes:
      - repo-cache:/tmp/repo_cache
    restart: unless-stopped

volumes:
  repo-cache:
