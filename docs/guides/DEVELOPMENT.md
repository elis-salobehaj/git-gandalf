# Development Guide

> **Coming in Phase 1.** This guide will cover the local development workflow (`bun run dev`, `bun test --watch`), testing strategy, Biome lint/format conventions (`bun run check`), and how to work against the active implementation plan in `docs/plans/active/`.
